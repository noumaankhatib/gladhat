import { WhenTalkHeroScene } from '../../components/three/WhenTalkHeroScene';
import type { IslandProps } from '../types';

export function WhenTalkHeroSceneIsland(_props: IslandProps) {
  return <WhenTalkHeroScene />;
}
